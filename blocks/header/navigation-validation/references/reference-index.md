# Reference Index — Navigation Orchestrator

| Category | Files |
|----------|-------|
| **Implementation flowchart** | Full flow from first file to desktop + mobile validated — see `references/implementation-flowchart.md` |
| **Workflow (gates & hooks)** | 6 flowcharts: 4 sub-agents, desktop/megamenu/mobile/critique + gates — see `docs/main branch - nav orchestrator workflow.md` |
| **Validation artifacts** | Phase JSON files under `blocks/header/navigation-validation/` — see `references/validation-artifacts.md` |
| **Workflow start message** | First user-facing message at nav orchestration start (intro + 10-step overview) — see `references/workflow-start-message.md`. SKILL mandates: read and output before session.json / Step 1. |
| **Troubleshooting** | Common issues, cause, action — see `references/troubleshooting.md` |
| **Pre-upload checklist** | Skill development guide Section 8 — see `references/pre-upload-checklist.md` |
| **Output schema** | `references/output-contract.json` |
| **Sub-agent schemas** | `references/desktop-navigation-agent-schema.json`, `references/mobile-navigation-agent-schema.json`, `references/megamenu-schema.json`, `references/validation-agent-schema.json` |
| **Structural comparison** | `references/structural-summary-schema.json`; `blocks/header/navigation-validation/scripts/compare-structural-schema.js` (threshold **95%**, `--output-register` flag) |
| **Style register** | `references/style-register-schema.json`; runtime: `blocks/header/navigation-validation/style-register.json` |
| **Schema register** | `references/schema-register-schema.json`; written by compare script |
| **Megamenu mapping** | `references/megamenu-mapping-schema.json`; runtime: `megamenu-mapping.json` (source) + `migrated-megamenu-mapping.json` |
| **Programmatic nav extraction** | `references/programmatic-nav-extraction.md` — extract from `__NEXT_DATA__` or DOM when full tree is pre-rendered and no hover-revealed content; for sidebar+right-panel use Playwright hover per item |
| **Programmatic row detection** | `blocks/header/navigation-validation/scripts/detect-header-rows.js` — MANDATORY Phase 1 script. Run `node blocks/header/navigation-validation/scripts/detect-header-rows.js --url=<source-url>`. Uses Playwright; writes phase-1 + `.row-detection-complete`. Gate blocks until run. |
| **Megamenu behavior register** | `references/megamenu-behavior-register-schema.json`; runtime: `megamenu-behavior-register.json`; written by `blocks/header/navigation-validation/scripts/compare-megamenu-behavior.js` |
| **Row elements mapping** | `references/row-elements-mapping-schema.json`; runtime: `row-elements-mapping.json` (source) + `migrated-row-elements-mapping.json`; every row, every element (logo, nav, CTA, search, locale, hamburger, icons). |
| **Row elements behavior register** | `references/row-elements-behavior-register-schema.json`; runtime: `row-elements-behavior-register.json`; written by `blocks/header/navigation-validation/scripts/compare-row-elements-behavior.js`; hover/click validation for all desktop row elements. |
| **Header appearance mapping** | `references/header-appearance-mapping-schema.json`; runtime: `header-appearance-mapping.json` (source, **Phase 2 — REQUIRED before header.css**) + `migrated-header-appearance-mapping.json`; includes **headerBackgroundBehavior** (defaultState, interactionState); header bar background/shadow on hover/click. |
| **Header appearance register** | `header-appearance-register.json`; written by `blocks/header/navigation-validation/scripts/compare-header-appearance.js`; source vs migrated header bar appearance match. |
| **Per-component critique** | `nav-component-critique/SKILL.md` — steps A–G; replaces external critique skills for header |
| **4 key-component critique subagents** | `nav-component-critique/key-component-agents/` — one .md per key component: `critique-top-bar-desktop.md`, `critique-nav-links-row-desktop.md`, `critique-mobile-header-bar.md`, `critique-mobile-menu-root-panel.md`. Step 14 mandates invoking 4 subagents in parallel, each with one of these instruction files. See `key-component-agents/README.md`. |
| **Visual style comparison** | `nav-component-critique/SKILL.md` Step E — PRIMARY method for style scoring; visual screenshot comparison with structured scoring rubric |
| **Mobile validation** | `mobile/` subdirectory — `mobile-schema-register.json`, `mobile-style-register.json`, `mobile-heading-coverage.json`, `mobile-behavior-register.json`, `mobile/critique/` |
| **Mobile structure detection** | `blocks/header/navigation-validation/scripts/detect-mobile-structure.js` — MANDATORY when phase-4 exists. Run at 375×812: `node blocks/header/navigation-validation/scripts/detect-mobile-structure.js --url=<source-url> [--validation-dir=...]`. Writes `mobile/mobile-structure-detection.json` (rowCount, rows with itemCount, topLevelMenuItemCount) and `.mobile-structure-detection-complete`. Same as desktop: programmatic row/item count before structural validation. When mobile has extra images/text, add to nav.plain.html mobile-only section and mobile missing-content-register. Schema: `references/mobile-structure-detection-schema.json`. |
| **Compare mobile structure** | `blocks/header/navigation-validation/scripts/compare-mobile-structural-schema.js` — Compares mobile-structure-detection.json (source) to migrated-mobile-structural-summary.json; writes mobile-schema-register. Usage: `node blocks/header/navigation-validation/scripts/compare-mobile-structural-schema.js .../mobile/mobile-structure-detection.json .../mobile/migrated-mobile-structural-summary.json --output-register=.../mobile/mobile-schema-register.json`. When migrated has more items than source (mobile-only content), ensure in nav.plain.html and mobile missing-content-register. |
| **Mobile dimensional gate** | `blocks/header/navigation-validation/scripts/mobile-dimensional-gate.js` — Live DOM width validation at 375×812. Run `node blocks/header/navigation-validation/scripts/mobile-dimensional-gate.js --url=<migrated-url> [--validation-dir=...]` after mobile implementation; opens hamburger and checks menu list + nav-item widths = viewport, edge-to-edge, chevron alignment, container chain, computed styles. Writes `mobile/mobile-dimensional-gate-report.json`. Exit 0 = pass. Mandatory before Step 13 / completion. See gates summary Section 7. |
| **Missing content register** | `missing-content-register.json` — when source has content not in nav.plain.html, create with `items` (location, description, resolved). Add content to nav.plain.html; extract the exact styles from the source site so we match them precisely. Hook blocks until all resolved. Schema: `references/missing-content-register-schema.json` |
| **Mobile missing content register** | `mobile/missing-content-register.json` — when mobile has extra items not in nav.plain.html, create with `items`. Add to nav.plain.html; extract the exact styles from the source site so we match them precisely; style with @media to show only at mobile breakpoint (hide on desktop). Hook blocks until all resolved. Schema: `references/mobile-missing-content-register-schema.json` |
| **Enforcement** | `hooks/nav-validation-gate.js` — PostToolUse gates (1–19) + Stop checks; logs tagged [DESKTOP]/[MOBILE]/[CRITIQUE]/[VIEWPORT]/[SEARCH]/[LOCALE]/[MISSING-CONTENT]/[PANEL-LAYOUT]/[HEADER-APPEARANCE]/[IMAGE-AUDIT] |
| **Gates & hooks summary** | `references/nav-validation-gates-and-hooks-summary.md` — Maps 4 key components, split-link, parallel subagents, 95% check, completion message, skipped-component critique, and **menuItemsWidthLayout** (Gate 14d, Phase 4) to gates/hooks; use to validate nothing is missing. |
| **Nav content validation** | `blocks/header/navigation-validation/scripts/validate-nav-content.js` — MANDATORY after every nav.plain.html write. Validates: **(1) at least 2 top-level section divs** — do not put all content in a single `<div>` (breaks header/DA); **(2)** image refs vs requirements, file existence, size > 0. Exit 0 = pass; writes `.nav-content-validated` marker. nav.md is not supported. |
| **Image audit** | `blocks/header/navigation-validation/scripts/audit-header-images.js` — MANDATORY after validate-nav-content.js. **Nav mode:** compares expected (phase-2/3 + megamenu-mapping) to actual in nav and on disk; writes `image-audit-report.json`, `.image-audit-passed`. **URL mode:** `--url=<url> --output=<path>` produces manifest (img/svg/bg-image). **Compare mode:** `--compare=source.json --against=migrated.json [--validation-dir=...]` requires migrated ≥ source; writes `.image-manifest-compare-passed`. Hook blocks (IMAGE_PARITY) when both manifests exist until compare passes. |
| **Phase 1 row/height** | detect-header-rows.js writes navLandmarkCount; overrides rowCount if nav landmarks > bands. When header height > 1.3× sum of row heights, writes heightMismatch, headerTotalHeight, detectedRowsHeight. Hooks: HEADER_HEIGHT_SANITY blocks if heightMismatch; ROW_LANDMARK_PARITY requires phase-2.rows.length ≥ phase-1.rowCount. |
| **Debug log** | `blocks/header/navigation-validation/debug.log` |

**Critique proof (hook-enforced):** Every validated component in `style-register.json` must have `critiqueReportPath`, `screenshotSourcePath`, `screenshotMigratedPath` (all existing on disk), and `critiqueIterations >= 1`. Self-assessed scores are rejected.
