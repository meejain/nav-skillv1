# Validation Artifacts — Required Location

All phase outputs MUST be written under the header block so you can confirm each validation step was followed.

**Base path:** `blocks/header/navigation-validation/`

| File | Written after | Contents |
|------|----------------|----------|
| `session.json` | Start of run | `sourceUrl`, `migratedPath`, `startedAt` (ISO timestamp), **`workflowStartMessageDisplayed: true`** (required; set only after displaying `references/workflow-start-message.md` to the user — hook blocks if missing), optional `runId` |
| `phase-1-row-detection.json` | Phase 1 (from script only) | Row detection: rowCount, navLandmarkCount, confidence, uncertainty, notes. When header height > 1.3× sum of row heights: heightMismatch, headerTotalHeight, detectedRowsHeight. **Do not write manually** — run detect-header-rows.js. |
| `phase-2-row-mapping.json` | Phase 2 | Row mapping JSON (rows, confidence, uncertainty, notes) |
| `phase-3-megamenu.json` | Phase 3 | Megamenu analysis JSON (includes hasImages; used for desktop implementation) |
| `phase-5-aggregate.json` | After Phase 3 (desktop) | Desktop aggregate: headerStructure, desktopMapping, megamenuMapping, validationReport, status; mobileMapping: "pending" |
| `phase-4-mobile.json` | Phase 4 (only after customer confirmation) | Mobile behavior JSON |
| `phase-5-aggregate.json` (updated) | After Phase 4 | Full contract with real mobileMapping |
| `migrated-structural-summary.json` | After desktop implementation | Structural summary **extracted from migrated page** (same shape as source; used for comparison) |
| `style-register.json` | Step 13 (built immediately before critique) | Per-component list. **4 key components** (top bar, nav links row [desktop]; mobile header bar, mobile menu root panel [mobile]) are `pending` then critiqued; all others `status: "skipped"`. **allValidated** when the 4 key components are validated (≥95% + critique proof). Remaining components can be critiqued later on request. Built together with mobile-style-register at Step 13. |
| `schema-register.json` | From compare script | Per-component schema match (source vs migrated); written by `compare-structural-schema.js --output-register`; **all items must be validated** before proceeding. |
| `megamenu-mapping.json` | Phase 3 deep analysis | Source megamenu per-item behavior: every trigger, panel item, sub-item with hover + click behavior. Conforms to `references/megamenu-mapping-schema.json`. |
| `migrated-megamenu-mapping.json` | After implementation | Same schema as `megamenu-mapping.json` but filled from the **migrated** site. Every item hovered and clicked to record actual migrated behavior. |
| `megamenu-behavior-register.json` | From compare script | Per-sub-item comparison: source vs migrated hover/click/styling. Written by `compare-megamenu-behavior.js`. **All items must be validated** before proceeding. Hook-enforced. |
| `row-elements-mapping.json` | After Phase 2 + desktop implementation | Per-element hover/click for **every** desktop header row element (logo, nav links, CTA, search, locale, hamburger, icons). Conforms to `references/row-elements-mapping-schema.json`. Every row, every element — not just main nav headings. |
| `migrated-row-elements-mapping.json` | After desktop implementation | Same schema as `row-elements-mapping.json` but filled from the **migrated** site. Each element hovered and clicked to record migrated behavior. |
| `row-elements-behavior-register.json` | From compare script | Per-element comparison: source vs migrated hover/click for all row elements. Written by `compare-row-elements-behavior.js`. **All items must be validated** before proceeding. Hook-enforced. |
| `header-appearance-mapping.json` | **Phase 2 (source) — REQUIRED before implementation** | Header bar appearance on hover/click. **Must include `headerBackgroundBehavior`** (defaultState, interactionState, classToggle, requiresBodyPaddingTop, textColorInversion). Conforms to `references/header-appearance-mapping-schema.json`. Hook blocks header.css until this exists. |
| `migrated-header-appearance-mapping.json` | After desktop implementation | Same schema as `header-appearance-mapping.json` but filled from the **migrated** site. |
| `header-appearance-register.json` | From compare script | Source vs migrated header appearance match. Written by `compare-header-appearance.js`. **allValidated: true** required when header-appearance-mapping exists. Hook-enforced. |
| `image-audit-report.json` | From audit-header-images.js | Expected vs actual header images: `expectedCount`, `actualCount`, `validatedOnDisk`, `expectedSlots`, `missingByLocation`, `missingOrEmpty`, `passed`. Used to identify where image gaps are (e.g. megamenu feature cards). |
| `.image-audit-passed` | From audit-header-images.js (on success only) | Marker so hook allows proceeding. Required after nav.plain.html exists; hook blocks until audit passes. |
| `source-image-manifest.json` | From audit-header-images.js --url (source) | Full image manifest from source URL: img, svg, background-image in header. summary: imgTags, svgTags, bgImages, total. Used with --compare. |
| `migrated-image-manifest.json` | From audit-header-images.js --url (migrated) | Same shape as source manifest, from migrated URL. |
| `.image-manifest-compare-passed` | From audit-header-images.js --compare (on success) | Marker when migrated total ≥ source and no missing images. Hook (IMAGE_PARITY) blocks when both manifests exist until this exists. |
| `image-manifest-compare-report.json` | From audit-header-images.js --compare | sourceTotal, migratedTotal, missingCount, missing[], passed. |
| `missing-content-register.json` | When source has content not in nav.plain.html | Records omissions: `{ "items": [{ "location": "Concessionárias dropdown", "description": "Dealer search form", "resolved": false }] }`. **Create when** you detect source content missing from nav.plain.html. **Update** when you add content: set `resolved: true`. When adding content, extract the exact styles from the source site so we match them precisely. Hook blocks until all resolved. Conforms to `references/missing-content-register-schema.json`. |
| `critique/{componentId}/` | During step 14 (targeted critique — 4 key components) | Per-component critique artifacts: `source.png`, `migrated.png`, `critique-report.json`. For the 4 key components, one subdirectory per component. Screenshots + report prove visual critique ran. |
| `mobile/missing-content-register.json` | When mobile has content not in nav.plain.html | Mobile-only omissions: `{ "items": [{ "location": "mobile menu drawer", "description": "Extra CTA", "resolved": false }] }`. **Create when** Phase 4 finds mobile-only content missing from nav.plain.html. Add to nav.plain.html in mobile-only section; extract the exact styles from the source site so we match them precisely; in header.css hide on desktop (display:none default), show in @media. Set resolved: true. Hook blocks until all resolved. Schema: `references/mobile-missing-content-register-schema.json`. |
| `mobile/mobile-structure-detection.json` | From script only (before mobile structural validation) | Programmatic row and item count at 375×812: rowCount, rows (itemCount, hasImages), topLevelMenuItemCount. **Do not write manually** — run `detect-mobile-structure.js`. Same idea as desktop detect-header-rows. |
| `mobile/.mobile-structure-detection-complete` | From detect-mobile-structure.js (on success) | Marker so hook allows mobile structural validation. Required when phase-4 exists. |
| `mobile/migrated-mobile-structural-summary.json` | After mobile implementation | Mobile structural summary extracted from **migrated** page at mobile viewport. **Same shape** as mobile-structure-detection.json (rowCount, rows[], topLevelMenuItemCount) for compare-mobile-structural-schema.js. |
| `mobile/mobile-schema-register.json` | From compare-mobile-structural-schema.js | Per-component mobile structure match (source from mobile-structure-detection.json vs migrated). Run: `compare-mobile-structural-schema.js mobile/mobile-structure-detection.json mobile/migrated-mobile-structural-summary.json --output-register=mobile/mobile-schema-register.json`. When mobile has extra items/images, add to nav.plain.html mobile-only section and mobile missing-content-register. |
| `mobile/mobile-style-register.json` | Mobile style validation | Per-component mobile style register (same schema as desktop; status may be `pending`|`validated`|`skipped`). Two key components critiqued: `key-critique-mobile-header-bar`, `key-critique-mobile-menu-root-panel`; rest skipped unless customer requests full critique. |
| `mobile/mobile-heading-coverage.json` | Mobile heading validation | Coverage report: every heading tested with allCovered flag |
| `mobile/mobile-behavior-register.json` | Mobile behavior validation | Per-component tap/click/animation match (same role as desktop megamenu-behavior-register) |
| `mobile/critique/{componentId}/` | During step 14 (targeted critique — 4 key components) | Mobile per-component critique artifacts: `source.png`, `migrated.png`, `critique-report.json` (for the 2 key mobile components) |

## Rules

- Create the directory if it does not exist.
- **Locale selector (when hasFlags=true):** Country names and flag image references MUST be in nav.plain.html. header.js must only READ that content from the nav DOM and implement behavior (open/close, selection). header.css handles layout and styling (e.g. multi-column grid). Do NOT put country names or flag URLs in header.js — validate-nav-content.js and the hook enforce this.
- **Image validation (validate-nav-content.js):** After nav.plain.html write, run the script. **ALL images MUST use** `<img src="images/filename.ext" alt="...">` in nav.plain.html. **Use relative paths only — no leading /** (e.g. `images/logo.svg`). Absolute paths like `/content/images/logo.svg` resolve to filesystem root and fail validation. The script checks: (1) image refs satisfy phase-2/3 requirements, (2) each local file exists on disk, (3) each file has size > 0 (no broken/empty downloads). On pass, writes `.nav-content-validated` with `{ imageCount, requirementCount, validatedOnDisk }`. Hook blocks until marker exists and script exit 0. nav.md is not supported.
- **Image audit (audit-header-images.js):** After validate-nav-content.js passes, run the image audit. It builds **expected** image count from phase-2 (rows with hasImages), phase-3 (megamenu/columns hasImages), and megamenu-mapping.json (panel items with hasImage, feature cards, featured area). It compares to **actual** images referenced in nav and on disk. If actual < expected, it writes `image-audit-report.json` with `missingByLocation` (e.g. "Megamenu \"Industries\" → Refining Technologies") so you know where the gap is. On pass, writes `.image-audit-passed`. Hook blocks until this marker exists; Stop check also requires image audit passed.
- **Image manifest compare (optional flow):** When using URL mode to produce `source-image-manifest.json` and `migrated-image-manifest.json`, run `audit-header-images.js --compare=source-image-manifest.json --against=migrated-image-manifest.json --validation-dir=blocks/header/navigation-validation`. On pass, writes `.image-manifest-compare-passed`. Hook (IMAGE_PARITY) blocks when both manifests exist until this marker exists.
- Write each file **immediately after** producing that phase's JSON; do not proceed to the next phase until the file is written.
- Paths are relative to workspace root (e.g. `blocks/header/navigation-validation/phase-1-row-detection.json`). Do not use `/workspace/` prefix.
- If a phase is skipped (e.g. no megamenu), still write the file with the valid schema-shaped JSON (e.g. zero/empty values and notes).

## File Existence Checklist

| After | Files that must exist |
|-------|------------------------|
| Run start | `blocks/header/navigation-validation/session.json` |
| Phase 1 | + `phase-1-row-detection.json` |
| Phase 2 | + `phase-2-row-mapping.json`, + `header-appearance-mapping.json` (source, with headerBackgroundBehavior) |
| Phase 3 | + `phase-3-megamenu.json` |
| Desktop aggregate | + `phase-5-aggregate.json` (mobileMapping may be `"pending"`) |
| Desktop implementation | + `header.js`, `header.css` (styled; megamenu images if present in source) |
| **Image audit** | After nav.plain.html + validate-nav-content.js: run `audit-header-images.js`; require `.image-audit-passed` and exit 0. Hook blocks until no gap between expected (phase-2/3 + megamenu-mapping) and actual images. |
| **Image manifest compare** | When both `source-image-manifest.json` and `migrated-image-manifest.json` exist: run `audit-header-images.js --compare=... --against=... --validation-dir=blocks/header/navigation-validation`; require `.image-manifest-compare-passed`. Hook (IMAGE_PARITY) blocks until compare passes. |
| **Megamenu behavior validation (step 4 — FIRST)** | Create `migrated-megamenu-mapping.json` (include `panelLayoutDetails` per trigger with **measured values**: measuredLeft, measuredRight, viewportWidth from getBoundingClientRect; **computed CSS**: cssPosition, cssLeft, cssWidth; **viewportsTested** at 1440, 1920, 1366, 1280, 1024px); run `compare-megamenu-behavior.js`; require `megamenu-behavior-register.json` → `allValidated: true`. Hook blocks if measured values missing. |
| **Header appearance validation (step 4c)** | Source `header-appearance-mapping.json` created in Phase 2. After implementation: create `migrated-header-appearance-mapping.json`; run `compare-header-appearance.js`; require `header-appearance-register.json` → `allValidated: true` |
| **Row elements behavior validation (desktop)** | Create `row-elements-mapping.json` (source: hover and click every row element — logo, nav links, CTA, search, locale, hamburger, icons in all rows). After implementation, create `migrated-row-elements-mapping.json`; run `compare-row-elements-behavior.js`; require `row-elements-behavior-register.json` → `allValidated: true`. Covers ALL rows and ALL elements. |
| **Structural schema validation (step 5 — SECOND)** | Extract from migrated page → `migrated-structural-summary.json`; run `compare-structural-schema.js` with **--output-register**; require **95%** and **schema-register.json** all validated |
| **Customer confirmation (step 7)** | **STOP. Request: "Desktop structural + behavior validated. Confirm to proceed to mobile."** |
| Phase 4 (mobile analysis) | + `phase-4-mobile.json` (includes `hamburgerAnimation`, `mobileMenuLayout`, `menuItemsWidthLayout`, `accordionBehavior` or `slideInPanelBehavior`) |
| Full aggregate update | Update `phase-5-aggregate.json` with mobileMapping |
| Mobile implementation | Extract exact styles from source. Update header CSS/JS for breakpoints, hamburger → cross animation, drawer/accordion/slide-in-panel |
| **Mobile structural validation** | `mobile/migrated-mobile-structural-summary.json` + `mobile/mobile-schema-register.json` → allValidated |
| **Step 13: Build style registers** | Build BOTH `style-register.json` (desktop) AND `mobile/mobile-style-register.json` (mobile). Include the **4 key components** with exact IDs (`key-critique-top-bar-desktop`, `key-critique-nav-links-row-desktop`, `key-critique-mobile-header-bar`, `key-critique-mobile-menu-root-panel`) as `status: "pending"`; all other components `status: "skipped"`. **Hook blocks** until desktop + mobile validation complete. Do NOT run critique until both registers exist. |
| **Step 14: Targeted critique (4 key components)** | **MANDATORY:** Invoke **4 critique subagents in parallel**, each with its instruction file under `nav-component-critique/key-component-agents/`. Launch 4 concurrent tasks in one turn. After all 4 reports exist, apply CSS fixes, then re-run any component still &lt; 95%. Set the 4 to `validated`; rest remain `skipped`. **Hooks:** PostToolUse gates block style-register/mobile-style-register writes if validated entries lack critique proof (report + screenshots on disk). Stop check blocks session end until all 4 key components have `status: "validated"`, `lastSimilarity >= 95%`, and proof; no component may be `pending`. Completion message tells customer only 4 were critiqued (time); they can request critique of skipped components by component ID. |
| **Final pre-confirmation gate** | All registers validated (desktop + mobile), hamburger animation confirmed, no unwanted overlays |
